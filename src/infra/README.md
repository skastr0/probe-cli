# Infrastructure placeholder

This folder reserves host-side integration adapters for Apple and local-process boundaries.

Expected future areas include:

- `apple/` command wrappers
- `bridge/` long-lived JSONL subprocess bridges
- `fs/` artifact storage adapters

The bootstrap keeps this as documentation only so the repository shape reflects the architecture without pretending those integrations have already been researched or implemented.
