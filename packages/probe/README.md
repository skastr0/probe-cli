# Probe

Native npm launcher for Probe.

Install with:

```bash
npm install -g @skastr0/probe
probe --version
probe doctor --output-json
```

This package installs a small Node.js wrapper and an optional platform package containing the native Probe binary for macOS. Probe still requires Apple's Xcode command line tooling for iOS simulator and device control.
